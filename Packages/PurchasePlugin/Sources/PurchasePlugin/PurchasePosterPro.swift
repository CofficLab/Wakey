import MagicKit
import SwiftUI

struct PurchasePosterPro: View {
    var body: some View {
        PurchaseViewDemo()
            .frame(height: 500)
            .frame(width: 500)
            .inDesktop()
            .colorScheme(.dark)
    }
}
